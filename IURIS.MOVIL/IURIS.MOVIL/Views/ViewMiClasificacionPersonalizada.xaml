<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:IURIS.MOVIL.Modelos_y_clases"
             mc:Ignorable="d"
             x:Class="IURIS.MOVIL.Views.ViewMiClasificacionPersonalizada">

    <NavigationPage.TitleView>
        <FlexLayout>

            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="5" >
                <FlexLayout Grid.Column="0" >

                    <local:CustomLabel x:Name="lblTitle" Style="{StaticResource TextoAMostrarColorBlanco}"/>
                </FlexLayout>

                <Image Grid.Column="1" x:Name="IMGBasura" Source="BBasura.png" WidthRequest="20" HorizontalOptions="End" HeightRequest="20" Margin="10,0,20,0" IsVisible="false">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
                    </Image.GestureRecognizers>
                </Image>
            </Grid>
        </FlexLayout>
    </NavigationPage.TitleView>


    <StackLayout>
        <CollectionView x:Name="clltionMiClasificacionPersonalziada" SelectionMode="Multiple" ItemsSource="{Binding MisArticulos}" Margin="10,5" SelectionChanged="clltionMiClasificacionPersonalziada_SelectionChanged">
            <CollectionView.EmptyView>
                <StackLayout>
                    <Label TextColor="Black" FontAttributes="Bold"  FontSize="Large" HorizontalOptions="Center">No se encontraron datos</Label>
                </StackLayout>
            </CollectionView.EmptyView>
                
            <CollectionView.ItemTemplate>
                <DataTemplate>

                    <Expander HorizontalOptions="Start" VerticalOptions="Center">
                        
                        <Expander.Header>
                    
                            <StackLayout Orientation="Horizontal" HorizontalOptions="Start" VerticalOptions="Center">
                                <Image Source="Mas.png" WidthRequest="30" HeightRequest="30" VerticalOptions="Center">
                                    <Image.Triggers>
                                        <DataTrigger TargetType="Image" Binding="{Binding Source={RelativeSource AncestorType={x:Type Expander}}, Path=IsExpanded}" Value="True">
                                            <Setter Property="Source" Value="menos.png"/>
                                        </DataTrigger>
                                    </Image.Triggers>
                                </Image>
                                <Label Text="{Binding NumArticulo}" FontSize="16" VerticalOptions="Center" Style="{StaticResource TextoAMostrarColorRojo}" Margin="5,0"></Label>

                                <local:CustomLabel Text="{Binding NombreArticulo}" FontSize="15" Style="{StaticResource TextoAMostrarColorRojo}" VerticalOptions="Center"></local:CustomLabel>
                            </StackLayout>
                        
                        </Expander.Header>
                        
                        <StackLayout>
                            <local:CustomLabel Text="{Binding Contenido}" FontSize="Subtitle" Style="{StaticResource TextoAMostrarColorRojo}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10"></local:CustomLabel>
                        </StackLayout>
                    
                    </Expander>

                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>