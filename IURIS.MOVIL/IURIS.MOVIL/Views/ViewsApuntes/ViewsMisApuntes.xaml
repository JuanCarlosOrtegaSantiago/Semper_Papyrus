<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="IURIS.MOVIL.Views.ViewsApuntes.ViewsMisApuntes" Title="Apuntes guardados">
    
    <CollectionView x:Name="clltionApuntes" SelectionMode="Single" ItemsSource="{Binding Apuntes}" SelectionChanged="clltionApuntes_SelectionChanged">

        <CollectionView.EmptyView>
            <Label FontAttributes="Italic" FontSize="Large" HorizontalTextAlignment="Center" Style="{StaticResource TextoAMostrarColorRojo}" Text="Aun no tienes ninguna clasificación"/>
        </CollectionView.EmptyView>

        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="5" SnapPointsAlignment="Center"/>
        </CollectionView.ItemsLayout>
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <SwipeView>
                    <SwipeView.RightItems>
                        <SwipeItems Mode="Reveal" >
                            <SwipeItemView Invoked="SwipeItemView_Invoked" BackgroundColor="{StaticResource PrimaryColor}" IsTabStop="True">
                                <FlexLayout WidthRequest="100" Padding="5" AlignItems="Center">
                                    <Image Source="BBasura.png" Margin="0,2,0,0" WidthRequest="25" HeightRequest="25"></Image>
                                    <Label Text="Borrar" Style="{StaticResource TextoAMostrarColorBlanco}"></Label>
                                </FlexLayout>
                            </SwipeItemView>
                        </SwipeItems>
                    </SwipeView.RightItems>
                    <StackLayout>
                        
                    <Label Text="{Binding Nombre}" Style="{StaticResource TextoAMostrarColorRojo}" FontSize="Title" VerticalOptions="Center" Padding="15,10"></Label>
                    </StackLayout>
                </SwipeView>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>