<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="IURIS.MOVIL.Views.ViewsMisClasificaciones" Title="Mis clasificaciones">
    <FlexLayout >
        
        <Button Text="Crear clasificación" Style="{StaticResource buttonSecundario}" FlexLayout.Shrink="0" FlexLayout.Basis="8%" FontSize="Title" Clicked="TapGestureRecognizer_Tapped" Margin="-5"/>

        <CollectionView BackgroundColor="White" x:Name="clltionClasificaciones" SelectionMode="Single" ItemsSource="{Binding Clasificaciones}" SelectionChanged="clltionClasificaciones_SelectionChanged" >
            <CollectionView.EmptyView>
                    <Label FlexLayout.Basis="7%" FontAttributes="Italic" FontSize="Large" HorizontalTextAlignment="Center" Style="{StaticResource TextoAMostrarColorRojo}">Aún no tienes ninguna clasificación</Label>
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems Mode="Reveal" >
                                <SwipeItemView Invoked="SwipeItemView_Invoked" BackgroundColor="{StaticResource PrimaryColor}" IsTabStop="True">
                                    <FlexLayout WidthRequest="100" Padding="5" AlignItems="Center">
                                        <Image Source="BBasura.png" Margin="0,2,0,0" WidthRequest="25" HeightRequest="25"></Image>
                                        <Label Text="Borrar" Style="{StaticResource TextoAMostrarColorBlanco}"></Label>
                                    </FlexLayout>
                                </SwipeItemView>
                            </SwipeItems>
                        </SwipeView.RightItems>
                        <StackLayout>
                            <Label FlexLayout.Basis="7%" Text="{Binding Nombre}" Style="{StaticResource TextoAMostrarColorRojo}" FontSize="Title" VerticalOptions="Center" Padding="15,10"></Label>
                        </StackLayout>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </FlexLayout>
</ContentPage>