<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="IURIS.MOVIL.Detail.PageArticulos">
    <ContentPage.Content>
        <CollectionView x:Name="cllctionArticulos" SelectionMode="Single" ItemsSource="{Binding ListaArticulos}" >
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackLayout Grid.Row="0" Orientation="Horizontal" HorizontalOptions="Start" VerticalOptions="Center">
                            <Label Text="{Binding NumArticulo}" FontSize="Large" TextColor="Black"></Label>
                            <Label Text="{Binding NombreArticulo}" FontSize="Large" TextColor="Black"></Label>
                        </StackLayout>

                        <Label Text="{Binding Contenido}" FontSize="Large" TextColor="Black" Grid.Row="1"></Label>

                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>